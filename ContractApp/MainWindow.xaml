<Window x:Class="ContractApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ContractApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100*"/>
            <ColumnDefinition Width="200*"/>
            <ColumnDefinition Width="100*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="26*"/>
            <RowDefinition Height="33*"/>
            <RowDefinition Height="33*"/>
            <RowDefinition Height="28*"/>
            <RowDefinition Height="299*"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="nameTextBox" Grid.Column="1" Grid.Row="1"></TextBox>
        <TextBox x:Name="phoneTextBox" Grid.Column="1" Grid.Row="2"></TextBox>

        <FlowDocumentReader Grid.Row="4" Grid.ColumnSpan="3">
            <FlowDocument>
               <Paragraph>
                    <Span>
                        <Bold Binding.TargetUpdated="{Binding ElementName=nameTextBox, Path=Text}"></Bold>
                    </Span>
                </Paragraph>
            </FlowDocument>
        </FlowDocumentReader>
        
    </Grid>
</Window>
